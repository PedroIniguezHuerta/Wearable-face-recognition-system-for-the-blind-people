In this directory are stored the trained models in yaml format.
This files are automatically generated either:
1. When registering a new user
2. At boot time when the application is loaded.

Note:
   To avoid any issues, this solution is provided with no datasets, 
however use registration procedure allows to generate new datasets from scratch.